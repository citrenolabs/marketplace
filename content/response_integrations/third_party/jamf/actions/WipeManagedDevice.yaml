default_result_value: ''
description: Remotely erase/wipe a managed computer using Jamf Pro ERASE_DEVICE MDM
    command.
dynamic_results_metadata:
-   result_example_path: resources/WipeManagedDevice_JsonResult_example.json
    result_name: JsonResult
    show_result: true
integration_identifier: Jamf
name: Wipe Managed Device
parameters:
-   default_value: ''
    description: The ID of the computer to erase (required)
    is_mandatory: true
    name: Computer ID
    type: string
-   default_value: ''
    description: Optional 6-digit PIN for the erase command (leave empty if not needed)
    is_mandatory: true
    name: PIN
    type: string
-   default_value: Default
    description: How the device should be obliterated during erase
    is_mandatory: true
    name: Obliteration Behavior
    optional_values:
    - Default
    - DoNotObliterate
    - ObliterateWithWarning
    - Always
    type: ddl
-   default_value: false
    description: Whether to enable return to service after erase (requires MDM profile
        data)
    is_mandatory: true
    name: Return to Service
    type: boolean
-   default_value: ''
    description: Base64-encoded MDM profile data for return to service (optional,
        only used if Return to Service is enabled)
    is_mandatory: false
    name: MDM Profile Data
    type: string
-   default_value: ''
    description: Base64-encoded WiFi profile data for return to service (optional,
        only used if Return to Service is enabled)
    is_mandatory: false
    name: WiFi Profile Data
    type: string
script: WipeManagedDevice.py
timeout_seconds: 300
type: containment
